# Секреты — никогда в репозиторий
.env
.env.*
!.env.example
env.sh
secrets/
*token*.json
*client_secret*.json
*credentials*.json
*service-account*.json

# Локальные данные прогона
cache/
data/
state.json
*.log

# Собранный бинарник
env-release-tracker

# macOS
.DS_Store

# Локальные требования, планы и контекст ИИ
/docs/
MEMORY.md
AGENTS.md
CLAUDE.md
GEMINI.md
.agents/
.codex/
.claude/
.cursor/
.cursorrules
.github/copilot-instructions.md

# Не заказывали в ТЗ — держим локально до решения техлида (вопрос №11:
# где крутится сервис и по каким шаблонам CI). Файлы остаются на диске.
/.gitlab-ci.yml
/.pre-commit-config.yaml

# Server OAuth persistent volume
google-token/
.oauth-*
